import { FromEngineMessage } from "../post-messages";
/**
 * A message that gets send by the engine when the scene environment is rotated.
 */
export interface EnvironmentRotationChangedMessage
  extends FromEngineMessage<"environment-rotation-change"> {
  payload: number;
}
