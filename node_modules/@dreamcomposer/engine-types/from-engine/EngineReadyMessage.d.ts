import { FromEngineMessage } from "../post-messages";

/**
 * A message that gets send by the engine when it's ready to receive commands.
 * You must not send any messages before receiving this message.
 */
export interface EngineReadyMessage extends FromEngineMessage<"engine-ready"> {
  payload: boolean;
}
