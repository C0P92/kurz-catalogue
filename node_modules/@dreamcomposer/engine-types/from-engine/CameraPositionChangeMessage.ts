import { CameraPosition } from "../Camera";
import { FromEngineMessage } from "../post-messages";

/**
 * Rotates the camera to a given vector.
 */
export interface CameraPositionChangeMessage
  extends FromEngineMessage<"camera-position-change"> {
  payload: CameraPosition;
}
