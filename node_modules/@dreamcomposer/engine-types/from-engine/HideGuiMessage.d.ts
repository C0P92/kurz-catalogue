import { FromEngineMessage } from "../post-messages";

/**
 * A message that gets send by the engine when it's showing something that
 * interferes with the GUI, e.g the inspector.
 */
export interface HideGuiMessage extends FromEngineMessage<"hide-gui"> {
  payload: boolean;
}
