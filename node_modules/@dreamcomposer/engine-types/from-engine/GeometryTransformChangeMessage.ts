import { GeometryTransform } from "../GeometryTransform";
import { FromEngineMessage } from "../post-messages";

/**
 * Event object if the position or rotation of the geometry changed
 */
export interface GeometryTransformChangeMessage
  extends FromEngineMessage<"geometry-transform-change"> {
  payload: GeometryTransform;
}
