import { ToEngineMessage } from "../post-messages";

/**
 * Explicitely sets full screen mode for the IFrame
 */
export interface SetFullscreenMessage
  extends ToEngineMessage<"set-fullscreen"> {
  /**
   * A boolean, describing the desired full screen state
   */
  payload: boolean;
}
