import { ToEngineMessage } from "../post-messages";

/**
 * Toggles VR.
 */
export interface ToggleVrMessage extends ToEngineMessage<"toggle-vr"> {
  /**
   * An empty object.
   */
  // TODO: Add way to explicitly set it (i.e., a boolean)
  payload: Record<string, never>;
}
