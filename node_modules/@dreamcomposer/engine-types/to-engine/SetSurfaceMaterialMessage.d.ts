import { MaterialDefinition } from "../Material";
import { ToEngineMessage } from "../post-messages";

/**
 * Loads and sets the surface material by its definition.
 */
export interface SetSurfaceMaterialMessage
  extends ToEngineMessage<"set-surface-material"> {
  /**
   * The definition of the surface material
   */
  payload: MaterialDefinition;
}
