import { ToEngineMessage } from "../post-messages";

/**
 * Resets the camera to its initial position.
 */
export interface ResetCameraPositionMessage
  extends ToEngineMessage<"reset-camera-position"> {
  /**
   * An empty object.
   */
  payload: Record<string, never>;
}
