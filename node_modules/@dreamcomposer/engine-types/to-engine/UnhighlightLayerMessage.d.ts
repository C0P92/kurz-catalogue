import { ToEngineMessage } from "../post-messages";

/**
 * Removes previously highlighting of a layer.
 */
export interface UnhighlightLayerMessage
  extends ToEngineMessage<"unhighlight-layer"> {
  /**
   * The name of the layer to remove highlighting.
   */
  payload: string;
}
