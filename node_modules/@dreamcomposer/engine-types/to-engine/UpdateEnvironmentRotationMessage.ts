import { ToEngineMessage } from "../post-messages";

/**
 * Rotates the environment to a given angle.
 */
export interface UpdateEnvironmentRotationMessage
  extends ToEngineMessage<"update-environment-rotation"> {
  payload: {
    /** Target rotation degree */
    rotation: number;
    /** Animation duration in seconds */
    transitionDuration?: number;
  };
}
