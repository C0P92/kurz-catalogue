import { ToEngineMessage } from "../post-messages";

/**
 * Translates the geometry so its lowest part touches the ground.
 * The current rotation of the object is not affected.
 */
export interface PlaceGeometryOnGroundMessage
  extends ToEngineMessage<"place-geometry-on-ground"> {
  payload: { transitionDuration?: number };
}
