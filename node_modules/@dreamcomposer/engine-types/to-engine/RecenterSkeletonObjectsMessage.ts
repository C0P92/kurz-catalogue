import { ToEngineMessage } from "../post-messages";

/**
 * A message to set the mesh skeleton states
 */
export interface RecenterSkeletonObjectsMessage
  extends ToEngineMessage<"recenter-skeleton-objects"> {
  payload: void;
}
