import { ToEngineMessage } from "../post-messages";

/**
 * Enable/disable framing animations
 */
export interface SetUseFramingAnimation
  extends ToEngineMessage<"set-use-framing-animation"> {
  /**
   * A boolean, setting current engine mode for framing animations
   */
  payload: boolean;
}
