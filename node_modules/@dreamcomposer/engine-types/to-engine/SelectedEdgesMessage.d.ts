import { ToEngineMessage } from "../post-messages";

/**
 * Selectes an array of edges.
 *
 * TODO: Where do edges come from? Are they part of the texture?
 */
export interface SelectedEdgesMessage
  extends ToEngineMessage<"selected-edges"> {
  /**
   * An array with the index of the edges to select.
   *
   * Example: `[8, 9, 13]`
   */
  payload: number[];
}
