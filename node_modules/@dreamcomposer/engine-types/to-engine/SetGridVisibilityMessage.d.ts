import { ToEngineMessage } from "../post-messages";

/**
 * A message to toggle the visibility of the grid.
 */
export interface SetGridVisibilityMessage
  extends ToEngineMessage<"set-grid-visible"> {
  payload: boolean;
}
