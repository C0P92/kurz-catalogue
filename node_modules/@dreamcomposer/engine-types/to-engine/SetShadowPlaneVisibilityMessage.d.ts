import { ToEngineMessage } from "../post-messages";

/**
 * A message to toggle the visibility of the grid.
 */
export interface SetShadowPlaneVisibilityMessage
  extends ToEngineMessage<"set-shadowplane-visible"> {
  payload: boolean;
}
