import { ToEngineMessage } from "../post-messages";

/**
 * Highlights the given layer.
 */
export interface HighlightLayerMessage
  extends ToEngineMessage<"highlight-layer"> {
  /**
   * The name of the layer to highlight (i.e., the key used in the
   * SetLayerMessage payload).
   */
  payload: string;
}
