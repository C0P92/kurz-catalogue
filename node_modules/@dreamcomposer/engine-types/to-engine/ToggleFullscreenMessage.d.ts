import { ToEngineMessage } from "../post-messages";

/**
 * Toggles whether or not the iframe should be fullscreen.
 */
export interface ToggleFullscreenMessage
  extends ToEngineMessage<"toggle-fullscreen"> {
  /**
   * An empty object.
   */
  // TODO: Add way to explicitly set it (i.e., a boolean)
  payload: Record<string, never>;
}
