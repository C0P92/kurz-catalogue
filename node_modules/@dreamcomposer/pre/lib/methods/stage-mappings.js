/**
 * Sets the stage mappings (i.e., how a texture is mapped to the geometry).
 *
 * @param dcController The DcController object obtained from the init function.
 * @param mappings The stage mappings with the stage name as the key.
 * @returns A promise that resolves when the stage mappings have been set.
 */
// TODO: Improve documentation
export async function setStageMappings(dcController, mappings) {
    await dcController.postMessage({
        channel: "set-stage-mappings",
        payload: Object.fromEntries(Object.entries(mappings).map(([stageName, mapping]) => {
            var _a;
            return [
                stageName,
                {
                    uvMapping: mapping.uvMapping,
                    subMeshUVArea: mapping.subMeshUVArea,
                    rotation: (_a = mapping.rotation) !== null && _a !== void 0 ? _a : 0,
                    visible: mapping.visible,
                },
            ];
        })),
    });
}
