import { DcController } from "../DcController.js";
/**
 * Sets the PPI.
 *
 * The PPI value is required to properly render textures that have a fixed size
 * (e.g., a micro-embossing with a 5x5mm pattern).
 *
 * @param dcController The DcController object obtained from the init function.
 * @param ppi The PPI value.
 * @returns A promise that resolves when the PPI value has been set.
 */
export declare function setPpi(dcController: DcController, ppi: number): Promise<void>;
