/**
 * If the viewer is loaded, this method will unload the viewer.
 *
 * @param dcController The DcController object obtained from the init function.
 */
export function unload(dcController) {
    return dcController.unload();
}
