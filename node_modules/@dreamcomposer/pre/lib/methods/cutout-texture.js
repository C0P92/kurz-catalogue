export async function setCutoutTexture(dcController, data) {
    return dcController.postMessage({
        channel: "set-cutout-texture",
        payload: data !== null && data !== void 0 ? data : "",
    });
}
