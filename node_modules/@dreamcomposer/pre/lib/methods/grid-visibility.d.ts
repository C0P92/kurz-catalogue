import { DcController } from "../DcController.js";
/**
 * Sets whether or not the grid is visible.
 *
 * By default, the grid is not visible.
 *
 * @param dcController The DcController object obtained from the init function.
 * @param visible Whether or not the grid should be visible.
 * @returns A promise that resolves when the grid visibility has been set.
 */
export declare function setGridVisibility(dcController: DcController, visible: boolean): Promise<void>;
