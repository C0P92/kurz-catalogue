import { DcController } from "../DcController.js";
/**
 * Define additional HTTP Request that the engine will send to all request (i.e. loading layer textures)
 * For example this can be used if the server/service expects an Authorization header
 * @param dcController The DcController object obtained from the init function.
 * @param headers The HTTP headers that will be added to all future requests.
 * @returns A promise that when the headers have been set.
 */
export declare function setCustomHeaders(dcController: DcController, headers: {
    [key: string]: string;
}): Promise<void>;
