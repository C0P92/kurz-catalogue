/**
 * Sets whether or not the shadow plane (i.e., the shadow under the object) is
 * visible.
 *
 * By default, the shadow plane is visible.
 *
 * @param dcController The DcController object obtained from the init function.
 * @param visible Whether or not the shadow plane should be visible.
 * @returns A promise that resolves when the shadow plane visibility has been
 *          set.
 */
export function setShadowPlaneVisibility(dcController, visible) {
    return dcController.postMessage({
        channel: "set-shadowplane-visible",
        payload: visible,
    });
}
