import { DcController } from "../DcController.js";
import { ExtendedBaseUrl } from "./base-url.js";
/**
 * A list of options that can be used to initialize Dreamcomposer.
 */
export interface DCOptions {
    /**
     * The access token for Dreamcomposer.
     *
     * Defaults to the return value of `getAccessToken()`.
     */
    accessToken?: string;
    /**
     * The base URL for the iframe.
     *
     * Defaults to the return value of `getBaseUrl()`.
     */
    baseUrl?: string | ExtendedBaseUrl;
}
/**
 * Initializes Dreamcomposer and returns an controller instance that's required
 * by other methods to interact with the iframe.
 *
 * @param element A div element.
 * @param options The options.
 * @returns The controller instance.
 */
export declare function init(element: HTMLDivElement, options?: DCOptions): DcController;
/**
 * Initializes Dreamcomposer and returns an controller instance that's required
 * by other methods to interact with the iframe
 *
 * @param id The id of a div element.
 * @param options The options.
 * @returns The controller instance.
 */
export declare function init(id: string, options?: DCOptions): DcController;
