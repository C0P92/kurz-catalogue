import { DcController } from "./DcController.js";
import { PointerEventMessage } from "@dreamcomposer/engine-types/from-engine/PointerEventMessage.js";
/**
 * Passes mouse events from the embedder to the engine, since the iframe does
 * not receive mouse events outside of the iframe.
 *
 * @internal
 */
export declare class MouseEventHandler {
    private dcController;
    private boundHandleMouseDown;
    private boundHandleMouseUp;
    private boundHandleMouseMove;
    constructor(dcController: DcController);
    addListeners(): void;
    removeListeners(): void;
    handlePointerEvent(event: PointerEventMessage): void;
    private handleMouseDown;
    private handleMouseUp;
    private handleMouseMove;
    private sendEvent;
}
