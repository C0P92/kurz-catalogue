/**
 * @internal
 */
export declare const waitingQueue: Map<string, Promise<void>[]>;
/**
 * Ensures that promises in the queue are executed in the correct order and
 * that the next promise is only started when the previous one has finished.
 *
 * @internal
 */
export declare function putInQueue(key: string, promise: () => Promise<void>): Promise<void>;
