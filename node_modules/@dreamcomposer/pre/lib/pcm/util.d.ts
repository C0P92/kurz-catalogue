/**
 * Disabled the PCM check for the access token.
 *
 * This is a temporary option to bypass the access token check and get all items
 * from the PCM. It will be removed in the future and the access token will be
 * required by the PCM backend.
 */
export declare function disablePcmAccessTokenCheck(): void;
/**
 * @internal
 * Fetches data from the PCM.
 *
 * @param url The url for the request.
 * @param accessToken The access token of the application.
 * @returns The response.
 */
export declare const fetchPcmData: (url: string, accessToken: string) => Promise<Response>;
/**
 * Checks that the given access token is not undefined.
 */
export declare function checkAccessToken(accessToken?: string): asserts accessToken is string;
/**
 * Get a list with all PCM items of the given type.
 *
 * @param type The item type, used in the url.
 * @returns A list with the native PCM items.
 */
export declare function _getPcmItems<T extends {
    id: string;
}>(type: string, accessToken?: string, baseUrl?: string): Promise<T[]>;
export declare function _invalidateCache(type: string, uuid?: string, accessToken?: string, baseUrl?: string): void;
/**
 * Gets a native PCM item by its id.
 *
 * @param type The item type, used in the url.
 * @returns The native PCM item with the given id.
 */
export declare function _getPcmItem<T extends {
    id: string;
    alias?: string;
}>(type: string, id: string, accessToken?: string, baseUrl?: string): Promise<T | undefined>;
