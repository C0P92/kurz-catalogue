import { NativePcmItem, PcmItem } from "./item.js";
/**
 * @internal
 */
export interface PcmFeature extends PcmItem {
    /**
     * A string to identify the feature.
     */
    flag: string;
}
/**
 * @internal The feature how it is returned by the PCM api.
 */
export interface NativePcmFeature extends NativePcmItem {
    flag: string;
}
/**
 * @internal
 * Checks if the given object is a PcmFeature.
 *
 * @param obj The object to check.
 * @returns True if the object is a PcmFeature, false otherwise.
 */
export declare function isPcmFeature(obj: unknown): obj is PcmFeature;
/**
 * @internal
 * Gets a list with all available features.
 *
 * @param accessToken The access token.
 * @param baseUrl The base url.
 * @returns A list with all available features.
 */
export declare function getFeatures(accessToken?: string | undefined, baseUrl?: string): Promise<PcmFeature[]>;
